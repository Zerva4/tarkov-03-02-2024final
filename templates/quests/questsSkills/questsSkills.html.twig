{% if quest.slug == 'the-huntsman-path-relentless' %}
    <div class="prop">
        <div class="name">{{ 'Навыки'|trans({}, 'front.quests') }}</div>
        <div class="val">+ 3{{ 'навыка'|trans({}, 'front.quests') }} снайперские винтовки</div>
    </div>
{% endif %}
{% elseif quest.slug == 'crisis' %}
    <div class="prop">
        <div class="name">{{ 'Навыки'|trans({}, 'front.quests') }}</div>
        <div class="val">+ 8{{ 'навыка'|trans({}, 'front.quests') }} здоровья</div>
    </div>
{% endif %}
{% else %}
{% endif %}